services:
  fastapi:
    build: 
      context: .
      dockerfile: Dockerfile
    command: gunicorn -w 4 -k uvicorn.workers.UvicornWorker app:app --bind 0.0.0.0:8080

    ports:
      - "80:8080" 
    environment:
      - ENVIRONMENT=production 
    volumes:
      - .:/app 

